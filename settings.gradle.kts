pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

plugins {
    id("io.opengood.gradle.settings") version "1.18.0"
}

rootProject.name = "rest-api-contracts"
